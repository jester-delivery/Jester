# API – variabile de mediu (copiază ca .env și completează)
# Producție: nu comita .env în git.

# Obligatoriu – conexiune Postgres
# Local: localhost. În Docker: folosește numele serviciului „postgres”
DATABASE_URL=postgresql://postgres:PAROLA_TA@localhost:5432/jester

# Obligatoriu – semnătura JWT (minim 32 caractere random în producție)
JWT_SECRET=un-secret-foarte-lung-si-random-schimbat-in-prod

# Obligatoriu – emailuri admin (separate prin virgulă), pentru /admin
ADMIN_EMAILS=admin@example.com

# Opțional – port (implicit 4000)
PORT=4000
