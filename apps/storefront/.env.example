# Storefront Next.js – variabile de mediu
# Copiază ca .env.local (dev) sau setează pe server la build (production).
# NEXT_PUBLIC_* se încorporează la build – după schimbare e nevoie de rebuild.

# URL-ul API-ului (obligatoriu în production)
# Dev: http://localhost:4000
# Production: https://api.jester.delivery (sau domeniul tău API)
NEXT_PUBLIC_API_URL=http://localhost:4000
